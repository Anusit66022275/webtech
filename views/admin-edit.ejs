<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>แก้ไขหนังสือ</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #74ebd5, #acb6e5);
            font-family: 'Arial', sans-serif;
        }
        .edit-card {
            max-width: 500px;
            width: 100%;
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease-in-out;
        }
        .edit-card:hover {
            transform: translateY(-5px);
        }
        .form-label {
            font-weight: bold;
            color: #333;
        }
        .btn-primary {
            background: linear-gradient(135deg, #3498db, #6ab0de);
            border: none;
            transition: all 0.3s;
        }
        .btn-primary:hover {
            background: linear-gradient(135deg, #2980b9, #5fa3d1);
        }
    </style>
</head>
<body class="d-flex justify-content-center align-items-center vh-100">

    <div class="edit-card">
        <h2 class="text-center text-primary">✏️ แก้ไขหนังสือ</h2>
        <form action="/admin/edit/<%= book.id %>" method="POST">
            <div class="mb-3">
                <label class="form-label">📖 ชื่อหนังสือ</label>
                <input type="text" class="form-control" name="title" value="<%= book.title %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label">📚 หมวดหมู่</label>
                <input type="text" class="form-control" name="genre" value="<%= book.genre %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label">💰 ราคา (บาท)</label>
                <input type="number" class="form-control" name="price" value="<%= book.price %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label">🖼 URL รูปภาพ</label>
                <input type="text" class="form-control" name="image" value="<%= book.image %>">
            </div>
            <div class="mb-3">
                <label class="form-label">📦 จำนวนสต๊อค</label>
                <input type="number" class="form-control" name="stock" value="<%= book.stock %>">
            </div>
            <div class="mb-3">
                <label class="form-label">📝 คำอธิบาย</label>
                <textarea class="form-control" name="description" rows="3"><%= book.description %></textarea>
            </div>
            <div class="d-grid">
                <button type="submit" class="btn btn-primary">✅ บันทึก</button>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/javascript/toast.js"></script>
    <script src="/javascript/transition.js"></script>
</body>
</html>
