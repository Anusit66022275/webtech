<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ตะกร้าสินค้า</title>
    <link rel="stylesheet" href="css/cart.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body>
    <header class="header py-3">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-4">
                    <h1><strong>NUI MANGA STORE</strong></h1>
                </div>
                <div class="col-md-4">
                    <div class="d-flex search-bar">
                        <input type="text" placeholder="ค้นหาหนังสือการ์ตูน" class="form-control me-2">
                        <button class="btn btn-danger"><i class="bi bi-search"> </i></button>
                    </div>
                </div>
                <div class="col-md-4 text-end mt-2">
                    <a href="/cart" class="btn btn-outline-light"><i class="bi bi-cart-plus-fill my-1"></i> ตะกร้าสินค้า</a>
                    <a href="/login" class="btn btn-outline-light"><i class="bi bi-person-workspace"></i> เข้าสู่ระบบ</a>
                </div>
            </div>
        </div>
    </header>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/home"><strong>NUI MANGA STORE</strong></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/Book"> หนังสือการ์ตูน </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">ประวัติการซื้อ</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h2>ตะกร้าสินค้า</h2>
        <% if (cart.length > 0) { %>
            <ul id="cart-items-list" class="list-group">
                <% cart.forEach((item, index) => { %>
                    <li class="list-group-item d-flex align-items-center">
                        <img src="/uploads/<%= item.image %>" alt="<%= item.title %>" style="width: 50px; height: 50px; margin-right: 10px;">
                        <div>
                            <h5><%= item.title %></h5>
                            <p><%= item.price %> บาท x <%= item.quantity %></p>
                        </div>
                        <button class="btn btn-danger ms-auto remove-item" data-index="<%= index %>">ลบ</button>
                    </li>
                <% }) %>
            </ul>
            <p class="mt-3"><strong>รวม: <span id="total-price"><%= totalPrice %></span> บาท</strong></p>
            <button id="checkout" class="btn btn-success">ชำระเงิน</button>
        <% } else { %>
            <p>ไม่มีสินค้าในตะกร้า</p>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="javascript/cart.js"></script>
</body>
</html>
